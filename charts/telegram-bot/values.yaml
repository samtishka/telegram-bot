image:
  repository: docker.io/samtishka/telegram-bot
  tag: "latest"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 8080

env:
  - name: TELEGRAM_TOKEN
    valueFrom:
      secretKeyRef:
        name: telegram-bot-secret
        key: token
